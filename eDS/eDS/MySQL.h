#pragma once
/*

#include "StdAfx.h"


class mysql{
public:
	MYSQL SQL;
	bool Connected;
	std::string Account;
	std::string Password;
	std::string DataBase;
	int DBResult;
	MYSQL_RES *res;
	MYSQL_ROW row;
	MYSQL_FIELD *field;
	std::vector<std::string> ResultText;
	std::vector<std::string> ResultDB;
	unsigned int num_fields;
	my_ulonglong num_rows;
	mysql();
	~mysql();
	int Connect(char* Acc,char *Pwd, char* DB);
	void Disconnect();
	bool Execute( char *Query , ...);
	int GetInt(LPTSTR ColName);
	bool GetString(LPTSTR ColName, LPTSTR pOutBuffer);
	float GetFloat(LPTSTR ColName);
	int FindIndex(LPTSTR ColName);
	void mysql::Close();
	int mysql::GetResult(int iIndex);

};
*/